import { FormatCfaPipe } from './format-cfa.pipe';

describe('FormatCfaPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatCfaPipe();
    expect(pipe).toBeTruthy();
  });
});
